<div class="row">
  <div class="col-md-12">
    
    <h1>Liste des Catégories</h1>
    
    <hr>
    
    <% if notice.present? %><p id="notice"><%= raw notice %></p><% end %>
    
    <%= link_to new_category_path, class: "btn btn-success" do %>
      <span class="glyphicon glyphicon-plus"></span> Nouveau
    <% end %>
    
    <table class="table">
      <thead>
        <tr>
          <th class="col-md-8">Nom</th>
          <th class="col-md-4">Options</th>
        </tr>
      </thead>
    
      <tbody>
        <% @categories.each do |category| %>
          <tr>
            <td><%= category.nom %></td>
            <td>
              <%= link_to category, class: "btn btn-primary" do %>
                <span class="glyphicon glyphicon-eye-open"></span> Voir
              <% end %>
              <%= link_to edit_category_path(category), class: "btn btn-warning" do %>
                <span class="glyphicon glyphicon-pencil"></span> Modifier
              <% end %>
              <%= link_to category, method: :delete, data: { confirm: 'Voulez-vous vraiment supprimer cette catégorie?' }, class: "btn btn-danger" do %>
                <span class="glyphicon glyphicon-remove"></span> Supprimer
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    
    <%= will_paginate @categories, renderer: BootstrapPagination::Rails %>
  </div>
</div>
